spring:
  datasource:
    url: jdbc:mysql://db:3306/mmc   # db: docker-compose에 정의한 서비스명, 3306: Docker 내 실행중인 MySQL이 이 포트를 통해 외부와 통신
    username: root
    password: Root88*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # db 스키마를 애플리케이션에 맞게 자동으로 설정

  server:
    port: 8080
